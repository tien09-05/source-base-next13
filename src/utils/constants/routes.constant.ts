const API_ROUTES = {
  USERS: '/users',
  LOGIN: '/auth/login',
};

const WEB_ROUTES = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  LOGIN: '/login',
};

const QUERY_KEYS = {
  GET_USERS: 'GET_USERS',
};

export { API_ROUTES, QUERY_KEYS, WEB_ROUTES };
